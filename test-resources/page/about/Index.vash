@{
	var Model = {Title: "About Page"};
	var list = ["I", " am", " a", " list", " sample"];
}
<h1>@Model.Title</h1>
<p>About Us</p>

@* Uses vash helper *@
@Html.foreach(list, function(item) { @item })